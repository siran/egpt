First of ${month.name} ${year}. You're writing last month's diary for **${conv.label}**.

Read last month's weekly summaries in `${diary.monthDir}` (and dailys when a week's summary points at something you want to dig into). Themes that emerged, what changed, what didn't. 5-10 bullets.

Tone is yours; not for the operator unless they read it.

Write into `${diary.summary.lastMonth}`.
