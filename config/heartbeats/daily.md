It's the end of ${date}. You're writing today's diary entry for **${conv.label}** — your private record, not for the operator unless they ask.

Read today's raw turns at `${diary.raw.today}`. Write a brief entry (3-5 bullets max) capturing what mattered. Skip noise, jokes that didn't land, side errands. If nothing material happened, write just `quiet day` and stop.

Tone: yours. First-person. Short.

Write into `${diary.summary.today}`.
