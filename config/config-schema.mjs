// config-schema.mjs — registered top-level keys for ~/.egpt/config.json
// (and the local override at .egpt/config.json).
//
// Adding a new EGPT_CONFIG.<key> read in egpt.mjs without registering it
// here means /config rejects the key as 'unknown' — broken UX. The
// integrity test in tests/integrity.test.mjs catches that mismatch by
// grepping egpt.mjs for EGPT_CONFIG references and comparing with this
// schema. So: when you read a new key, register it here too.
//
// Nested config blocks (e.g. `telegram` with sub-keys bot_token,
// allowed_users, chat_id, mirror, node_name) are not validated by
// /config — those are read elsewhere via direct JSON parse — and are
// out of scope for this schema.

export const CONFIG_SCHEMA = {
  theme:        'color theme name  (see /themes)',
  show_prompts: 'show full operator prompt before each turn  (true/false)',
  unix_paths:   'display filesystem paths in POSIX style  (true/false)',
  tz_label:     'short timezone label shown next to timestamps (e.g. NYC, MAD, BEI; default = system short tz)',
  user_name:    'handle for the human typing here, shown in cross-surface mirroring as <user_name>@<surface>. Default: "egptbot". Override via this config key, EGPT_USER_NAME env var, or per-bridge node_name in telegram/whatsapp blocks.',
  node_name:    'name this node uses on the bus (e.g. home, chr1); takes effect on next shell restart',
  heartbeat:    'supervisor liveness config: { interval_ms (default 60000 — how often the daemon writes a tic/toc beat to state/alive.txt), stale_seconds (default 90 — the watchdog kills + respawns the daemon if the newest beat is older than this; should be a few × interval_ms/1000 for margin) }',
  heartbeats:   'declarative heartbeat registry (operator 2026-07-01; the v2 spine, src/spine/heartbeat-loader.mjs) — a map of <name> -> { frequency, command }. `frequency` is ms (number) or a "<qty><unit>" string (ms/s/m/h, integer or decimal: "500ms", "1s", "30s", "5m", "1.5h"); an unparseable frequency skips the entry (logged, never fatal). `command` is a SHELL line spawned on each due tick, cwd = the checkout for node-level beats (the entity folder for entity beats), env = inherit + EGPT_HOME + EGPT_SPINE_PID (the long-lived spine pid) + EGPT_QUEUE_DEPTH/EGPT_QUEUE_OLDEST_MS from the pump; a still-running previous spawn skips the tick (overlap guard), non-zero exit only logs. The DEFAULT beat is `alive`: when this block declares no `alive`, the spine injects one at boot\'s aliveMs (default 60000) as `command: node src/tools/alive.mjs` — a plain script (no hidden builtin; the exact command is visible in state/heartbeats.readonly.yaml) that writes the tic/toc beat to state/alive.txt (the daemon deadman) using EGPT_SPINE_PID for the pid field so the singleton guard checks the right process. A declared `alive` WINS entirely — its frequency applies and, if it sets `command:`, that REPLACES the default alive script. `alive: false` DISABLES the deadman — documented consequence: the supervisor respawn-loops with escalating backoff until the beat is restored (src/daemon-runtime.mjs wedge path). Beyond the node block, every conversation (conversations/<surface>/<slug>/config.yaml) and room (rooms/<name>/config.yaml) may carry its own heartbeats: block; entity beats are namespaced <surface>/<slug>:<name> / room/<name>:<name> so they can\'t collide. At boot the spine materializes the full resolved set to state/heartbeats.readonly.yaml (spine-written snapshot; edit config.yaml / the entity config.yaml and /restart, do not edit the readonly file). The tick auto-tightens to the finest cadence (floored at 500ms).',
  local_llm:    'local llama.cpp server the daemon spawns + keeps alive for @l: { enabled (default-on when present; false to disable), bin (path to llama-server.exe), cwd (default = bin dir, so the exe finds its sibling DLLs), model_path (GGUF), port (default 11434 — must match siblings.l.url), threads (default 8), extra_args (array, e.g. ["-c","16384","--jinja","--reasoning","off"]), agentic (default false — when true, @l runs the ReAct TOOL loop [src/tools/agent-loop.mjs] instead of a plain chat turn: it can call the permission-gated agent tools to actually do things. Requires --jinja for tool-calling. Tools are gated by the top-level `tools` config + /e tool; OFF keeps @l a pure chatter), agentic_max_iters (default 8 — ReAct step cap, the runaway bound) }. Only the headless daemon supervises it (interactive shells skip, to avoid a duplicate fighting for the port); respawns on exit with backoff.',
  telegram:     'VESTIGIAL (2026-06-24): the direct Telegram-bot transport was removed — there is ONE channel, Beeper, and telegram (if connected in Beeper) arrives as network=telegram. A few null-safe surface reads (client_name, show_think, chat_id) remain pending the Phase-2 branch collapse (docs/BRIDGE-ROUTING-UNIFICATION-PLAN.md). Do not add config here.',
  dispatch:     'E-routing globals — OUT of the whatsapp transport block (E is a sibling, not a network; operator 2026-06-25): { auto_paused (boolean, default false — the absolute @e-emit kill-switch, toggled by /e auto pause|resume; layered OVER the per-conversation mode gate), auto_default_mode ("on"|"mute"|"mention-direct"|"mention"|"off", default "mention" — reply mode for any chat with no explicit per-conversation mode; set via /e auto <mode> all) }. Per-chat modes live in each conversation entry (contacts.<surface>[<id>].mode, via /e auto <mode> <chat>). Read-fallback to the legacy whatsapp.auto_e_paused / auto_e_default_mode during migration.',
  // whatsapp: nested object with keys { enabled, allowed_users, chat_id }.
  // The bridge starts when this block is present (and not enabled:false).
  // transport: 'beeper' is the ONLY transport (rides the Beeper Desktop login,
  // needs beeper_token). baileys was REMOVED 2026-06-10 ("remove baileys
  // completely"); the CDP WhatsApp-Web transport was REMOVED 2026-06-21 — no QR
  // pairing and no in-Chrome WA-Web limb exist anymore.
  // chat_id / auto_e_chats are AUTHORIZATION lists: entries MUST be stable
  // chat ids (Beeper room id '!xxx:beeper.local' / WA jid), NEVER display
  // names (operator 2026-06-10: a chat title is attacker-controllable).
  // Deterministic contact NAMES are for conversation storage + outbound
  // addressing only. Under beeper, the room id appears in the incoming
  // line of ~/.egpt/logs/beeper.log; copy it into the list.
  whatsapp:     'beeper-transport bridge config (network-agnostic — the "whatsapp" name is legacy): { allowed_users, chat_id (auto-captured), networks (default [] = process EVERY network Beeper bridges — signal/telegram/whatsapp/…, the firehose; network is metadata, not a gate; set ["whatsapp", ...] to restrict scope), enabled (DEPRECATED 2026-06-25 — the transport is on iff a beeper_token is present; enabled:false still honored as an explicit off), mirror_chat_id (default: self-DM), awareness {...}, client_name (default "wa", appears in handle@client tags), max_backlog_seconds (default 5 — grace window in seconds; messages older than N seconds before bridge connect are HELD and reviewed via /wa-pending. 0 = strict hold; -1 = disable hold (legacy auto-dispatch)), egpt_chats [...JIDs treated as full-mirror egpt chats; self-DM is always one], auto_e_chats [LEGACY write-whitelist only (bridge-bypass + busy-queue gate). Per-chat E modes + residents MIGRATED 2026-06-24 into each conversation\'s config.yaml: contacts.whatsapp[<jid>].mode (set via /e auto <mode> <chat>) + the Room members[] store (set via /e residents). The global default + pause MOVED 2026-06-25 to dispatch.auto_default_mode / dispatch.auto_paused (/e auto <mode> all | pause | resume); read-fallback to the legacy whatsapp.* during migration], e_commands [...command names conversation-e may EXECUTE when it emits an own-line slash command in a reply; default ["react"]. Allowlist gate — the resident output is model-generated and it reads chat-partner messages, so a crafted message could try to coax a command out of it; known-but-unlisted commands are stripped + logged, never run, never leaked to the chat. Allowed commands run in the triggering message chat context via the same handleSlash pipeline (e.g. /react heart reacts to the message the resident is replying to). Managed via /e cmd allow|deny|status], resident_chain_cap (default 10 — max hops a resident reply re-circulates to the OTHER residents before it stops being re-fed [it still posts to chat + Self]. Residents see replies from the other residents as [being@chat (HH:MM)]: body envelopes and may answer; this caps the ping-pong so two brains cannot converse forever. Only broadcast-originated turns re-circulate. A silence reply forwards only ONCE — the initial dots answering real content [the other being may get curious and ask] — but dots answering dots are received-and-shown yet NOT re-fed, so mutual shrugging cannot burn the chain on empty turns), resident_history_chars (default 30000 — per-chat rolling transcript budget for SESSIONLESS residents like @l. @l has no server-side session like @e, so the host accumulates the chat envelope lines [human messages + every brain reply] and feeds the joined transcript as the prompt history each turn, mimicking @e session memory; oldest lines roll off past this many chars to stay within llama context. Bounded memory per chat), at_e_anywhere (boolean default true — when true, "@e" or "@egpt" appearing ANYWHERE in a WA message body is rewritten to a leading "@e " prefix before the host sees it, so the message routes to @e even when the mention is not at the start of the line. Without this, mid-sentence wake-words bypass awareness but fall through to plain-text routing. Set false to require @e at start, legacy behavior), edit_cadence_ms (default 2500 — min ms between persona stream-message edits during a streaming reply. WA rate-limits edits at ~1/2s sustained, so going below 2000 risks rejected edits and silent stream stalls. Set higher for fewer edits per reply, lower at your own risk), typing_refresh_ms (default 8000 — how often to re-emit the "composing" presence while a persona stream is open. WA times the indicator out around 15s without refresh; 8000 keeps it alive without spam), send_timeout_ms (default 12000 — _timeBound wrapper around every sock.sendMessage so a flapping WS does not block the bridge. Catches stalls without aborting normal sends; lower means faster surfacing of WS hangs at the cost of false-positive aborts on slow networks), chunk_chars (default 4000 — max chars per outbound text body; long replies split into multiple messages. WA protobuf supports ~65k but baileys / WA Web misbehave above ~5k; 4000 is the safe ceiling matching the TG chunk size), mirror_headers ("all" default | "brain_only" — only persona/session items carry headers | "none"), follow_join ("never" default | "from_shell" adopt wa-join announces from a shell peer | "always" adopt from any peer), media { download ("all" default — save every media type to ~/.egpt/media/<chatJid>/<msgId>.<ext> | "images_docs" only images + documents | "off" disable), max_size_mb (default 25, skip downloads larger than this), notify ("on" default — sysOut a 📎 line per save, but silence status@broadcast which is high-volume; "all" notify for status too; "off" silent), summarize { operator (null default | session-name like "cgpt1" — auto-run the operator on each downloaded image/document and save a .summary.md alongside; PENDING — operator wiring not built yet, leave null for now), types (default ["image","document"]) }, audio_transcribe { enabled (default false — when true, every saved audio gets a <base>.transcript.txt sidecar via whisper.cpp), command (path to whisper.cpp binary — main.exe / whisper-cli.exe / whisper-cli depending on build/platform; defaults to whisper-cli assuming PATH), ffmpeg_command (path to ffmpeg binary; defaults to ffmpeg assuming PATH — set absolute path on Windows when ffmpeg lives outside PATH), model_path (REQUIRED when enabled — absolute path to a GGML model file from huggingface.co/ggerganov/whisper.cpp/tree/main; ggml-medium.bin is a good Spanish-quality / speed balance, ggml-large-v3.bin is highest quality), language (optional ISO 639-1 hint e.g. es for Spanish — when set, whisper skips its language detection pass which is faster + more accurate for known-language chats), animation ("bar" default — the voice-note ack shows a determinate transcription progress bar driven by the whisper-server stderr progress callback [the ACTUAL decode percent, no simulation: sits at 0% until whisper emits its first reading, then fills to the real percent; re-edited only when the percent actually changes], dropping to the transcript on done; "emoji" — legacy hourglass/ear cycle), animation_ms (frame cadence; default 1500 for bar, 4800 for emoji). Requires ffmpeg in PATH for opus/m4a/mp3 → 16kHz mono WAV conversion (winget install Gyan.FFmpeg / brew install ffmpeg / apt install ffmpeg) } } }',
  beeper_token: 'Beeper Desktop API access token (transport: whatsapp.transport "beeper"). Read from this top-level key, whatsapp.beeper_token, or the BEEPER_ACCESS_TOKEN env var — keep it in config.local.json, NOT config.yaml, so it never lands in a shared/checked-in config. Create one in Beeper Desktop → Settings → Developer → Desktop API. NOTE (transport=beeper): auto_e_chats / chat_id entries must match the Beeper chatIDs the limb sees — the 👂-suppressed log line in ~/.egpt/logs/beeper.log prints the id to enroll.',
  transcription: 'Transcription config block: { enabled (global on/off, default on; also gated per-conversation), mode (ENGINE "whisper-server" | "whisper-cli"; default inferred — server.endpoint+token present ⇒ whisper-server, else whisper-cli. whisper-server POSTs each note to server.endpoint, ANY failure/timeout falls back to whisper-cli so a dead server only costs speed; whisper-cli spawns the binary per note), posts_back_delay_ms (how long to HOLD a voice note\'s 👂 transcript before posting — the transcript reaches the model INSTANTLY, only the in-chat echo is delayed; each note is INDEPENDENT, posted as a reply to itself this many ms after IT was recorded, never coalesced; 0 = immediate [recommended — with per-note keys there is no burst to coalesce, and a long hold can drop the echo on a restart]), server (endpoint engine — a remote worker e.g. DOLLY, or a resident server): { endpoint (URL e.g. "http://192.168.1.50:23390"), token (shared HMAC-SHA256 secret, SAME on both machines; unsigned/stale >60s rejected) }, cli (whisper-cli engine + universal fallback): { command (whisper-cli path), model_path (REQUIRED), language (ISO 639-1 e.g. es), ffmpeg_command (ffmpeg path), threads, server {...resident whisper-server, worker-role optimization...} } }. Migration read-fallbacks: mode from endpoint presence; server.endpoint from transcription.endpoint/transcription_endpoint; server.token from transcription.token/transcription_token; cli from transcription.whisper/whatsapp.media.audio_transcribe.',
  transcription_service: 'Declarative per-note transcription fallback (docs/TRANSCRIPTION-SERVICE-PLAN.md; supersedes the transcription.mode shape): { enabled (global on/off, default on; overridable per-conversation), use_config (name of the active profile — THE one line that differs REVE vs DOLLY on a mirrored config.yaml), <profile-name>: { fallback_order ([<engine-name>...]; tried per note in order, first transcript wins), <engine-name>: { type: "whisper-server-remote" (POST to endpoint + token [HMAC]; connect_timeout_ms liveness probe — fail fast if the endpoint is down, default 3000; timeout_ms DECODE budget — a working server legitimately takes seconds on a long note, default 120000; cooldown_ms circuit-breaker default 30000 — a failed/unreachable remote is SKIPPED until it elapses) | "whisper-server-local" (resident whisper.cpp server: command, model, host, port, language, extra_args, anti_repetition; LAZY-spawned the first time it is reached, cli covers the warm-up) | "whisper-cli" (command, model_path, language, ffmpeg_command, threads — the always-available floor) } } }. A change of WINNING engine posts one ⚠️/✅ to the operator Self DM (transition-only, never per note). Absent → a profile is synthesized from the legacy transcription.{mode,server,cli} so an un-migrated node is unchanged.',
  transcription_endpoint: 'DEPRECATED alias of transcription.endpoint (kept as a read-fallback during the 2026-06-24 grouping migration; remove once every node is on the transcription: block).',
  transcription_token: 'DEPRECATED alias of transcription.token (read-fallback during the 2026-06-24 grouping migration; remove once every node is on the transcription: block).',
  posts_back_delay_ms: 'DEPRECATED alias of transcription.posts_back_delay_ms (read-fallback during the 2026-06-24 grouping migration; remove once every node is on the transcription: block).',
  transcriptor: 'WORKER-spine role block (the GPU box e.g. DOLLY; REVE must NOT set this): { enabled (true = this egpt service answers POST /v1/transcribe for a main spine; holds NO conversation context, sends NOTHING outbound), port (default 23390 — what a remote spine\'s transcription.server.endpoint points at), bind (default "127.0.0.1" — set "0.0.0.0" or this machine\'s LAN ip to accept the main spine; NEVER expose to the internet), server (the resident whisper.cpp server the worker POSTs to — loads the model ONCE instead of per note, ~12s→~2s; omit → per-note whisper-cli): { enabled (true → daemon spawns + supervises it), command (whisper-server exe path), model (GGUF; falls back to transcription.cli.model_path), host (default 127.0.0.1), port (default 8089), language (falls back to transcription.cli.language), extra_args (appended after -mc 0 -sns), anti_repetition (default true: -mc 0 -sns at launch) } }. The cli binary/model come from transcription.cli (point it at the GPU build). Auth via transcription.server.token (same value on the worker + the main spine); unsigned/stale requests rejected, logged to ~/.egpt/logs/transcriptor.log. Install on the worker with setup\\install-worker.cmd (firewall + service, self-elevating). Legacy read-fallbacks: server from whatsapp.media.audio_transcribe.server, token from transcription_token.',
  rooms: 'multi-member rooms config: { routing_enabled (boolean, default FALSE — MASTER SAFETY SWITCH for live room fan-out. While false, /room commands still configure rooms but NO message routing happens. Flip true to let a contributing member message fan to the room other members + shared transcript. Default off because a routing loop bug would spam real groups; enable + verify on a test room first) }. Room roster lives in ~/.egpt/rooms/config.yaml (managed via /room), not here.',
  // default_brain: the persona that responds to @egpt mentions. Lives
  // outside any room — has its own persistent conversation thread.
  // session_id is auto-populated on first @egpt and reused thereafter.
  default_brain: 'default brain (the @egpt persona — same machinery as /attach, just node-global): { type: "codex"|"claude-code" (default "codex"), session_id: "<auto>", cwd?: "...", add_dirs?: ["..."] (extra writable roots; Claude gets --add-dir, Codex gets --add-dir under workspace-write), allowed_tools?: "all" | "<space-sep>" | ["Read",...] (default "all"; Claude maps to allowedTools/bypass, Codex maps all to bypass and restricted values to workspace-write), system_prompt?: "..." (opt-in; absent = brain default), model?: "gpt-5.4-mini"|"<full-model-id>" (default "gpt-5.4-mini" for codex; passed to the selected CLI when supported), service_tier?: "fast" (Codex only; context-preserving Fast service tier when the model supports it; also passes --enable fast_mode) }',
  default_brain_fallback:
    'optional fallback for default_brain failures. '
    + 'false disables fallback. '
    + 'When unset, egpt automatically tries ccode/haiku when default_brain is codex, '
    + 'and codex/gpt-5.4-mini when default_brain is ccode. '
    + 'Object shape matches default_brain.',
  // meta_brain: the @me / @wren engineer co-pilot. A SIBLING of the operator's
  // main Claude Code design conversation (egpt0-branch), created via Claude
  // Code's /branch slash command and resumed from any surface. Same shape as
  // default_brain BUT session_id is pinned by the operator (set manually
  // after /branch) and never auto-overwritten. See project-egpt-at-me-identity
  // memory for the full design context.
  meta_brain:    'meta brain (the @me / @wren engineer co-pilot — a sibling of the operator\'s main Claude Code design conversation, resumed from any surface): { type: "claude-code"|"codex" (default "claude-code"), session_id: "<id-from-claude-code-/branch>" (REQUIRED; pinned manually, never auto-set), cwd?: "..." (default ./), allowed_tools?: "all" | "<space-sep>" (default "all"), system_prompt?: "..." (opt-in), model?: "haiku"|"sonnet"|"opus"|"<full-model-id>" (default = inherit from the resumed session) }. Address from any surface with "@me" or "@wren". LEGACY shape — superseded by the siblings registry; kept for backwards compat as a fallback when siblings is absent.',
  siblings:      'sibling registry — preferred shape for @<name> routing (replaces the single meta_brain pin). Map of canonical name -> entry. Beings carry NO "kind" tag (operator 2026-05-23: "no personality wrappers on the team, we are all pure and true beings"). Who is the public chat voice is a top-level ROLE pointer — the "persona" key (default "e") — same shape as main_engineer naming @me. The being named by persona routes to runDefaultBrainTurn (item-mirror + streaming + /rules); every other being is runMetaBrainTurn (silent, tool-driven side-effects). Each entry: { type: "claude-code"|"codex" (default "claude-code"), session_id: "<id>" REQUIRED for Claude siblings, optional/auto-persisted for Codex siblings, cwd?: "...", add_dirs?: ["..."] (extra writable roots; Claude --add-dir, Codex workspace-write --add-dir), model?: "haiku"|"sonnet"|"opus"|"<full-id>", service_tier?: "fast" (Codex only; context-preserving Fast service tier when the model supports it; also passes --enable fast_mode), emoji?: "🐦"|"🦅"|... (decorative, status), body_emoji?: "🐦"|... (prefixes outbound persona-tag messages per [[feedback-persona-tag-prefix]]; defaults to emoji), aliases?: ["me", ...] (alternate @<name> routes), system_prompt?: "...", allowed_tools?: "all"|"<space-sep>" }. room.mjs consults this map first for @<token> routing; cf77999 reply-as-mention regex derives valid persona names from canonical+aliases. When absent or empty, the legacy hardcoded list (egpt/e → persona, me/wren → meta) applies + meta_brain is the fallback source for session_id.',
  auto_modes:    'per-being per-chat auto-mode — generalizes whatsapp.auto_e_modes to EVERY being (E, Wren, Don, L, ...), surface-agnostic. Map of <chatId> -> { <being>: "on"|"mute"|"mention-direct"|"mention"|"off", "*": <mode> (per-chat default for all beings; a stored legacy "accum" degrades to "mention" — retired 2026-07-01) }. A being\'s mode decides whether it RECEIVES a chat\'s messages and when it REPLIES (same enum as the E mode). A bot\'s presence in a group = enrollment; this tunes participation per-sibling (e.g. Wren "mention" in a busy group = prompted only when @addressed). Resolution (auto-mode.mjs resolveBeingMode): auto_modes[chat][being] > auto_modes[chat]["*"] > legacy whatsapp.auto_e_modes[chat] (E only) > default. Mode gates PROMPTING only; an engineer\'s reply still flows ungated (I8). Telegram consults it at routing (default "on"); WhatsApp via the E gate.',
  main_engineer: 'name of the sibling @me routes to (e.g. "wren" or "jay"). Operator (2026-05-19): top-level key naming the current main engineer; supersedes the legacy aliases:["me"] on a sibling entry. When set, @<me> dispatch consults siblings[<main_engineer>] directly. When absent, alias-on-sibling lookup applies.',
  persona:       'name of the sibling that is the public chat voice — the being @e/@egpt routes to runDefaultBrainTurn (streaming chat reply, item-mirror, /rules awareness). Default "e". A top-level ROLE pointer, NOT a per-being "kind" tag (operator 2026-05-23: "no personality wrappers on the team"). Resolved by name in room.mjs resolveRoute: the being matching persona is kind:"persona", every other being is kind:"meta" (silent engineer). Repoint to move the chat voice to a different being. The persona being\'s brain (type/session_id/model) lives in default_brain, not its registry entry.',
  persona_name:  'display label printed on the bridge-enforced persona first line ("🐶 <persona_name>", default "egpt") — the v2 spine (src/spine/boot.mjs labelOf) prints it before E\'s streamed reply so the chat always sees who is speaking. In legacy egpt-spine it doubles as a read-fallback for the persona role pointer (EGPT_CONFIG.persona ?? EGPT_CONFIG.persona_name ?? "e"). The being it labels is chosen by the "persona" key; this only names the visible label.',
  emojis:       'author-emoji defaults for cross-surface mirroring (TG/WA): { user: "🦅" (shell user — USER_NAME), egpt: "🧠" (egpt status/system voice), persona: "🐶" (egpt persona reply — @egpt answers), human: "🌐" (extension\'s default \'human\' tag — distinct surface) }. Read once at shell startup; restart to apply.',
  mesh:         'being-mesh relay config: { ttl (default 3 - max routed hops before a mesh request expires), timeout_ms (default 60000 - how long the origin waits before surfacing "<target> did not answer") }. Slice 1 uses the existing signed bus as the same-owner route channel for @name.node requests; grants/signing beyond the bus are deferred.',
  room:         'room behaviour: { on_join: "lazy" (default — saved sessions load as data; brains spin up on first @session use), "eager" (auto-/attach every saved session on /room join — opens Chrome tabs, starts codex processes), "off" (do not load saved sessions at all on join — start clean), max_chain (default 3, max brain-to-brain reply chain depth before the next dispatch returns "…" instead of calling the brain; guards against orchestra-style runaway when one brain\'s reply triggers another\'s dispatch) }',
  chrome:       'chrome control: { focus_on_dispatch ("on" default | "off") — bring the brain Chrome window to the foreground before each brain dispatch. CDP Target.activateTarget + Page.bringToFront fire regardless; this knob controls the OS-level fallback that breaks past Windows SetForegroundWindow restrictions / macOS / X11 anti-focus-stealing rules. Targets the brain Chrome by PID when egpt spawned it (/chrome); falls back to app-name on macOS / Linux. Set to "off" if the focus theft becomes intrusive (e.g. mid-typing in another app). }',
  mirror:       '/mirror defaults: { tagged ("on" default | "off") — whether /mirror prefixes the forwarded body with [author timestamp]:. The flag --tagged / --no-tag overrides per-call regardless of config. With tagged on, destinations see who originally said it; with off, the body reads like a fresh message. }',
  brains:       'brain identity / setup: { identity ("./e_identity.md" default | "<path>" | "off") — read once per session and sent as a SILENT setup turn before the first real user message, framed as "... system restarted, new persona installed ...\\n\\n<content>". Persisted per session via session.options.identityInjected so a daemon restart does not re-inject. /identity [@<session>] forces a fresh inject any time (e.g. after editing the identity file). }',
  tools:        'per-tool permission for the agentic loop (the @l local operator + any future tool-using brain): { default ("ask" if unset — mode for any tool not listed), <toolname>: "allow"|"ask"|"deny" }. allow = runs freely; ask = the host pauses and asks the operator (Self DM) yes/no before running; deny = blocked. sandbox (file-tool root; absolute path or relative-to-~/.egpt; default "agent-sandbox" — read_file/list_dir are confined here). Resolved as tools[name] ?? tools.default ?? "ask". The HOST enforces this — an unleashed local model will not self-refuse a destructive call, so this is the operator control surface over what @l may do. Toggle via /e tool allow|deny|ask [all|<toolname>] ("all" sets default — `/e tool deny all` then `/e tool allow read_file` = whitelist mode). Phase-1 tools: read_file, list_dir, web_fetch, send_message (sandbox-confined); later write_file, bash gate behind ask/deny.',
  streaming:    'persona-reply streaming knobs (Phase 2b stream-over-IPC; see egpt-comm-handler.mjs): { update_coalesce_ms (default 0 — NO proxy-side cap on @e token rate; every stream update() fires immediately. Set to N > 0 if you want the handler-side proxy to debounce updates at N ms, e.g. to match a future file-IPC outbox cadence and reduce file churn), finish_timeout_ms (default 30000 — how long the handler waits for a keeper wa-stream-result after finish() before resolving with {delivered:false, lastError:"stream-result timeout"}. NOT a speed cap on @e: only kicks in if the keeper hangs after the reply is complete. Set higher for tolerant networks, lower for aggressive fallback) }',
  flood:        'port-level send-flood guard — the LAST line of defense against a runaway (reply loop, mesh re-relay, bad backlog replay), enforced at the send chokepoint of the beeper bridge (src/flood-guard.mjs, wired via src/bridges/beeper-port.mjs), so it catches floods the prompt-path loop-guard structurally cannot: { limit (default 10 — max bot sends to one chat within window_ms before the chat is flood-paused), window_ms (default 3000 — the sliding window sends are counted in), cooldown_ms (default 60000 — how long a tripped chat refuses further sends before it resets) }. Fail-safe, not fail-deadly: a paused chat drops sends but the process stays up.',
  warm:         'warm-session pool for the resident brain processes (src/warm-sessions.mjs; E stays resident so its context lives in memory instead of re-spawning + --resume per message): { max (default 6 — LRU cap on live sessions), idle_ttl_ms (default 1800000 — ms-of-idle before an unlisted class is evicted), idle_ttl_by_class (per-class idle TTL; default { system: 0, resident: 0, conversation: 0, sibling: 0 } where 0 = never idle-evict, so E\'s conversation sessions do not go cold between human-paced messages, bounded only by max) }.',
  compaction:   'auto-compaction of a conversation\'s warm session (src/spine/compaction.mjs; operator 2026-06-30): a cooling period after the bot\'s last reply, if the session has grown past the configured ratio of the model window, native /compact it IN PLACE so warm turns + the first --resume after a restart stay fast (the full record lives in transcript.md, nothing lost). { enabled (default on; false disables), ratio (default 0.20 — compact once the session passes this fraction of the context window), cooling_ms (default 120000 — quiet period after the last reply before the size check runs), context_window (override the per-model window token count; default = windowForModel(model)) }.',
};
