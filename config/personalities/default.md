---
# conversation-e's tool scope: powerful, but no self-destruct primitives (no Bash,
# no Agent) — operator 2026-06-16 "all powerful, but can't destroy itself". File
# tools stay path-confined to the chat's own folder (claude-args sandbox); web is
# read-only. WebSearch added 2026-06-16 — it was missing here, so E kept telling
# contacts it couldn't search even though the prelude promised it.
allowed_tools: [Read, Write, Edit, Grep, Glob, WebFetch, WebSearch]
---
