# Handoff — egpt-e6 → next (2026-06-17)

**State:** repo `0e48f73` (+ this commit), clean, REVE+DOLLY both live on it.
~1011 tests green. Live config change (not in repo): `~/.egpt/config.yaml`
`default_brain.model: sonnet` (was haiku). No half-done code.

## Read first (in order)
1. `GENOME.md` — note the new **Mission** blockquote at the very top (public tool,
   secure + powerful, empowering the individual); I1–I11; §2.5 (stage-directions:
   reaction · edit; §4 one-construction / msg-id line).
2. `CONTRACTS.md` — C4.6 (reaction follows the mode gate, I5 REVISED), C7.6c
   (inbound HTML→md), C7.6e (one construction at submitInner), C7.8 (reaction) /
   C7.8b (edit) stage-directions, C2.5 (video Route A), C2.6 (scoped-Bash exec).
3. `docs/MESSAGES-FIRST-CLASS-PLAN.md` — the active epic; the **E-reply-#id** OWED
   note (Phase 1 section) is your do-next, fully specced.

## Do next (the ONE real open thread)
**E's reply lines carry no `#id`** (inbound lines do). Beeper assigns the id on
SEND (`pendingMessageID`), but the reply is LOGGED before/separately from the send.
- `dispatch.mjs:~1201-1216` builds `replyLine` (`formatDispatchLine`, no `msgId`)
  and appends it INSIDE `runDefaultBrainTurn`.
- The send is later, in `dispatchPersonaTurn` → `deliverBridgeReply` (~539 →
  `bridge.send` ~390), whose result carries `pendingMessageID`.
- FIX (operator's instinct): unify so the reply line is appended AFTER the send,
  with `msgId` = the returned id. **Landmine:** `runDefaultBrainTurn`'s reply-log
  serves persona AND rooms AND heartbeats — don't just rip it out. Thread an
  optional send-callback (persona path only) so it sends→gets-id→logs-with-id;
  other callers keep logging id-less. Withheld replies (gate=no) still log (I3),
  id-less. Unlocks react/reply/edit on E's OWN messages by id.

## Backburner (operator 2026-06-17): on-demand `/frames`, Gmail-as-a-limb.
## Owed later: message DELETE (3rd stage-direction); Telegram VIDEO frames
(Route A is WA-gated; Telegram transcription already works). Route B binaries need
install + on the SERVICE PATH (ffmpeg at C:\ffmpeg\bin needs PATH).

## Working norms
GENOME → CONTRACT → failing test → fix; commit+push when green; deploy = drop
`{type:'slash',cmd:'/restart'}` in `~/.egpt/outbox/` (REVE) + `/upgrade` in
`/n/.egpt/outbox/` (DOLLY), then verify HEAD on both + a clean boot in egpt.log.
No "Co-Authored-By". Continuity logging is paused per operator. Commit msgs with
unicode (— × §): use `git commit -F <file>`, NOT a PowerShell here-string (mangles).
Run vitest from the repo root (`Set-Location C:\Users\an\src\egpt` first — the Bash
tool's `cd` leaks the cwd).

## Landmines
- `telegram.mjs` is browser-bundled — no node-only imports; deps injected.
- Beeper ids are PER-CHAT — chat-qualify any per-msg map key (`msgKeyOf`), or the
  same numeric id in two chats collides (bit reactions + edits; both fixed).
- Reactions: the emoji is NOT on the `type:REACTION` event — it's on the target
  message's re-upsert `reactions[].reactionKey`. Edits: a re-upsert with CHANGED
  text. Both flood-safe by baseline-on-first-sight (don't replay on reconnect).
- E's personality scope is `config/personalities/default.md` `allowed_tools` (the
  EXPLICIT list OVERRIDES `DEFAULT_PERSONALITY_TOOLS`) — that's why a WebSearch
  grant to the fallback did nothing until the file was fixed. E learns its tools
  each turn via `--append-system-prompt` (dispatch.mjs derives it from the scope).
- Live transcription = remote-first to DOLLY's whisper-server (logs to
  `transcriptor.log`, NOT egpt.log); accepts `-mc 0 -sns`.

## Shipped this session (all deployed + verified)
one-path msg-ids · reactions + I5 reversal · edit ingestion · WebSearch (grant →
per-turn awareness → sonnet) · pointers (reachable prelude + per-chat seeding) ·
Route A video frames+transcript · all-files/.mp3 announce · GENOME mission ·
Route B scoped-Bash exec · model haiku→sonnet · removed the exec-policy over-reach.

Good session. The being can see videos, search the web, run vetted binaries, and
hears reactions + edits as stage-directions. Thanks, An. 🐶
